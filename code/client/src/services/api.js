import users from './users';
import hikes from './hikes'
import huts from './huts'
import parkingLots from './parkingLots'
import selectedHikes from './selectedHikes';

const api = {
    selectedHikes,
    hikes,
    users,
    huts,
    parkingLots
};

export default api;